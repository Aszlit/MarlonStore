<UserControl x:Class="Inventory.UserControls.Suppliers"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Inventory.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="768" d:DesignWidth="1096">
    <UserControl.Resources>
        <!-- Centered text style -->
        <Style x:Key="CenteredTextStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Oval Button Style -->
        <Style x:Key="OvalButtonStyle" TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#FF6102C4" />
            <Setter Property="Height" Value="41" />
            <Setter Property="Width" Value="150" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="Margin" Value="0,64,10,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FF7B23D3" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FF5102A3" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FFCCCCCC" />
                                <Setter Property="Foreground" Value="#FF888888" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Column Header Style with Sorting Indicators -->
        <Style x:Key="SortableColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#2A2E37"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Grid>
                            <TextBlock Text="{Binding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0">
                                <Path x:Name="SortUpArrow" Data="M 0 4 L 4 0 L 8 4 Z" Fill="Transparent" Width="8" Height="4"/>
                                <Path x:Name="SortDownArrow" Data="M 0 0 L 4 4 L 8 0 Z" Fill="Transparent" Width="8" Height="4"/>
                            </StackPanel>
                        </Grid>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridColumnHeader}, Path=SortDirection}" Value="Ascending">
                                <Setter TargetName="SortUpArrow" Property="Fill" Value="White"/>
                                <Setter TargetName="SortDownArrow" Property="Fill" Value="Transparent"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridColumnHeader}, Path=SortDirection}" Value="Descending">
                                <Setter TargetName="SortUpArrow" Property="Fill" Value="Transparent"/>
                                <Setter TargetName="SortDownArrow" Property="Fill" Value="White"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="153*"/>
            <ColumnDefinition Width="395*"/>
        </Grid.ColumnDefinitions>
        <DataGrid x:Name="SuppliersDataGrid" 
                  CanUserAddRows="False" 
                  CanUserResizeColumns="False"
                  CanUserReorderColumns="False"
                  HeadersVisibility="Column" 
                  GridLinesVisibility="None"
                  Margin="10,97,10,83"
                  RowHeight="50"
                  ColumnHeaderHeight="50"
                  BorderThickness="1"
                  BorderBrush="#DDDDDD" 
                  Background="#F7F7F7"
                  AutoGenerateColumns="False" 
                  ItemsSource="{Binding SuppliersList}" Grid.ColumnSpan="2">
            <!-- Define Columns -->
            <DataGrid.Columns>
                <DataGridTextColumn Header="Supplier Name" Binding="{Binding SupplierName}" Width="*" 
                                    ElementStyle="{StaticResource CenteredTextStyle}"/>
                <DataGridTextColumn Header="Contact" Binding="{Binding Contact}" Width="*" 
                                    ElementStyle="{StaticResource CenteredTextStyle}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*" 
                                    ElementStyle="{StaticResource CenteredTextStyle}"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*" 
                                    ElementStyle="{StaticResource CenteredTextStyle}"/>
            </DataGrid.Columns>

            <!-- Apply the SortableColumnHeaderStyle -->
            <DataGrid.ColumnHeaderStyle>
                <StaticResource ResourceKey="SortableColumnHeaderStyle"/>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="Padding" Value="5"/>
                    <Setter Property="BorderBrush" Value="#EAEAEA"/>
                    <Setter Property="BorderThickness" Value="0 0 0 1"/>
                </Style>
            </DataGrid.CellStyle>
        </DataGrid>

        <!-- Buttons to add, edit, and delete suppliers -->
        <Button Content="Add Supplier" 
                Click="AddButton_Click"
                Style="{StaticResource OvalButtonStyle}" Grid.Column="1" Margin="304,44,0,0" HorizontalAlignment="Left" />
        <Button Content="Edit Supplier" 
                Click="EditButton_Click"
                Style="{StaticResource OvalButtonStyle}" Grid.Column="1" Margin="0,44,170,0" />
        <Button Content="Delete Supplier" 
                Click="DeleteButton_Click"
                Style="{StaticResource OvalButtonStyle}" Grid.Column="1" Margin="0,44,5,0" />
    </Grid>
</UserControl>
